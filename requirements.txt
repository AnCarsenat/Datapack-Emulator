# Runtime dependencies for the window, for running from a checkout without
# installing. The usual setup is `pip install -e ".[gui]"`; the emulator core
# needs none of these.
PySide6>=6.6
pyqtgraph>=0.13
numpy>=1.26
