the window will use PySide6
graphs will use pyqtgraph (for performance cost etc)
the function call view could use (NetworkX)


running the project
```sh
# run from project root / :
python ./src/main.py
```